;
; comments are single-line: semicolon-started
;

    mov 1, r0       ; move 1 into register 0
    mov 2, r1       ; move 2 into register 1

hello:

    add r0, r1      ; add r0+r1, store into r0

    jmp label

